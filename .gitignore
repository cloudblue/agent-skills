__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
.DS_Store
*.swp
.idea/
.vscode/

# Codex marketplace lives in .agents/ — un-ignore it (global gitignores often exclude it)
!.agents/
# …but vendored skills pulled in by the `skills` CLI are not ours to ship
.agents/skills/
skills-lock.json

# Local working notes and review artifacts — never versioned
SKILLS_PLAN.md
.connect-review/
LOCAL_TESTING_STATUS.md
SKILLS_TEST_PLAN.md
SKILLS_TEST_RESULTS.md
seed_connect.py
